# control-server

Open coordination plane: network / device / invite / peer / route / relay-directory. Reuses Axum+SeaORM+PG18 patterns; no billing.

Coordination plane: networks/devices/invites/peers/routes/IPAM + `/api/v1`.
