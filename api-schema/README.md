# api-schema

OpenAPI schema + protocol docs + protocol/epoch version definitions (client/server/relay_protocol_version, min_supported_*, config epoch).

control-server API contract: `openapi.yaml` + protocol/version specification.
