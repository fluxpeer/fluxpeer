# relay-server

Self-built relay (DERP-style). Forwards ciphertext only; auth + rate-limit + health + region directory. Designed with reference to iroh-relay (see NOTICE).

DERP-style relay, ciphertext-only, doubles as the STUN responder.
